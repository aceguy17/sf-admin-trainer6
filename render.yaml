services:
  - type: web
    name: sf-admin-trainer-pro
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn salesforce_pro.main:app --host 0.0.0.0 --port $PORT
    plan: free
